# actionlint only knows GitHub-hosted labels; register the self-hosted ARC
# runner scale set (infra/docs/04-arc-and-caching.md) so `runs-on: airis-kata`
# lints clean.
self-hosted-runner:
   labels:
      - airis-kata
